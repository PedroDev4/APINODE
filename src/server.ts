import { app } from './app';


app.listen(3333, () => console.log('Server is running!'));

/**
 * GET -> BUSCAR;
 * POST -> SALVAR;
 * PUT -> ATUALIZAR;
 * DELETE -> DELETAR;
 * PATCH -> Alteração Especifica;   
 */